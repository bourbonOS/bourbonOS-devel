type: dnf
repos:
  copr:
    enable:
      - chronos/main
install:
  packages:
    - atuin
    - distrobox
    - firefox
    - unzip
    - plymouth 
    - plymouth-theme-spinner
remove:
  packages:
    - toolbox
