#!/bin/bash

if [[ "$UID" -ne 0 ]]; then
    echo "Error: Not authorized."
    exit 1
fi

set -oue pipefail

mkdir -p /var/usrlocal/homefs
chown root:root /var/usrlocal/homefs
chmod 700 /var/usrlocal/homefs