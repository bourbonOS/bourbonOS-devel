[Unit]
Description=Set Transit ID
DefaultDependencies=no
Before=gdm.service

[Service]
RemainAfterExit=true
ExecStart=/bin/echo "$(openssl rand -hex 8)" > /etc/transit/transit-id
ExecStop=/bin/echo "Not initialized" > /etc/transit/transit-id

[Install]
WantedBy=multi-user.target